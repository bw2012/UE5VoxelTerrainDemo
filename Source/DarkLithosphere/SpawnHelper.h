#pragma once

#include "CoreMinimal.h"
#include "BaseCharacter.h"

bool IsCursorPositionValid(const FHitResult& Hit);

//void CalculateTargetRotation(ABaseCharacter* Character, FRotator& Rotation);

//void CalculateTargetRotation(APawn* Pawn, FRotator& Rotation);

//bool TestNewConstruction(UWorld* World, TSubclassOf<ASandboxObject> NewObjSubclass, const FHitResult& TraceResult, FVector& NewLocation, FRotator& NewRotation);

//void CalculateCursorPosition2(APawn* Pawn, const FHitResult& Res, FVector& Location, FRotator& Rotation, ASandboxObject* Obj);

//AActor* SpawnSandboxObject(UWorld* World, const FVector& Location, const FRotator& Rotation, ASandboxObject* Object, bool bEnablePhys = false);

//FRotator SelectRotation();

